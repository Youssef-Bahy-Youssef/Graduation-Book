% =============================================================================
% NexaLearn Graduation Project — Package Imports
% Cairo University, Faculty of Engineering, Department of Computer Engineering
% =============================================================================

% ---- XeLaTeX — Fonts & Arabic Support ----
\usepackage{fontspec}
\setmainfont{Helvetica}
\setsansfont{Helvetica}
\renewcommand{\familydefault}{\sfdefault}

\usepackage{polyglossia}
\newfontfamily\arabicfont[Script=Arabic,AutoFakeBold=2.5]{DecoType Naskh}
\newfontfamily\arabicfontsf[Script=Arabic,AutoFakeBold=2.5]{DecoType Naskh}
\newfontfamily\arabicfonttt[Script=Arabic]{DecoType Naskh}
\setdefaultlanguage{english}
\setotherlanguage{arabic}

% ---- Page Layout ----
\usepackage{geometry}
\geometry{
  a4paper,
  top=2.5cm,
  bottom=2.5cm,
  left=3cm,
  right=2.5cm
}

% ---- Line Spacing ----
\usepackage{setspace}

% ---- Section & Chapter Formatting ----
\usepackage{titlesec}

% ---- Headers & Footers ----
\usepackage{fancyhdr}

% ---- Table of Contents Formatting ----
\usepackage{tocloft}

% ---- Graphics & Floats ----
\usepackage{graphicx}
\usepackage{float}
\usepackage{placeins}

% ---- Captions (figures below, tables above) ----
\usepackage{caption}
\usepackage{subcaption}

% ---- Tables ----
\usepackage{array}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{multirow}
\usepackage{tabularx}
\usepackage{amssymb}

% ---- Math ----
\usepackage{amsmath}

% ---- Colors ----
\usepackage[table]{xcolor}
\definecolor{CUGreen}{HTML}{006400}

% ---- Code Listings ----
\usepackage{listings}
\lstset{
  basicstyle=\ttfamily\small,
  breaklines=true,
  frame=single,
  numbers=left,
  numberstyle=\tiny\color{gray},
  backgroundcolor=\color{gray!10},
  captionpos=b,
  tabsize=2,
  morekeywords={async, await, injectable, readonly, constructor, private, protected, public, static, implements, extends},
}
\lstdefinelanguage{TypeScript}{
  keywords={typeof, new, true, false, catch, function, return, null, catch, switch, var, if, in, while, do, else, case, break, const, let, for, export, import, from, as, type, interface, enum, class, extends, implements, async, await},
  ndkeywords={string, number, boolean, any, never, void, unknown, Record, Partial, Required, Readonly, Pick, Omit, Promise, Observable, Inject, Injectable, Module, Controller, Get, Post, Put, Patch, Delete, Body, Param, Query, Headers, Req, Res, Next, HttpCode, HttpStatus, UseGuards, UsePipes, UseInterceptors, SetMetadata},
  sensitive=true,
  comment=[l]{//},
  morecomment=[s]{/*}{*/},
  morestring=[b]',
  morestring=[b]",
  alsoletter={:,@},
}[keywords, comments, strings]
\lstalias{TypeStyle}{TypeScript}
\renewcommand{\lstlistlistingname}{List of Listings}

% Default language for all listings
\lstset{language=TypeScript}

% ---- Hyperlinks ----
\usepackage{hyperref}

% ---- Bibliography ----
\usepackage[
  backend=biber,
  style=numeric,
  sorting=none,
  maxbibnames=99,
  language=american
]{biblatex}
\DeclareLanguageMapping{arabic}{american}

% ---- Microtype (better justification, fewer overfull boxes) ----
\usepackage{microtype}

% ---- Justified body text ----
\usepackage{ragged2e}
